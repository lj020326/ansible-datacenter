{{ ansible_managed|comment }}
MASTER="{{ jenkins_swarm_agent_master|default('') }}"
JAR="{{ swarm_jar.dest }}"
MASTER_PORT="{{ jenkins_swarm_agent_master_port }}"
MASTER_PROTOCOL="{{ jenkins_swarm_agent_master_protocol }}"
SWARM_USERNAME="{{ jenkins_swarm_agent_username }}"
SWARM_PASSWORD="{{ jenkins_swarm_agent_password }}"
SWARM_PASSWORD_FILE="{{ jenkins_swarm_agent_password_file }}"
NAME="{{ jenkins_swarm_agent_name }}"
NUM_EXECUTORS="{{ jenkins_swarm_agent_num_executors }}"
LABELS="{{ jenkins_swarm_agent_labels }}"
