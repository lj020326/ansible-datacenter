---

__docker_stack__proxyspec__wordpress_backend: "{{
  docker_stack__wordpress__backend | d('wordpress.example.int:8080') }}"

__docker_stack__proxyspec__wordpress:
  proxy_config: "{{ __docker_stack__wordpress__proxy_config | d({}, true) }}"
