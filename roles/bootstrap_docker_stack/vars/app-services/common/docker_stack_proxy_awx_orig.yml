---

__docker_stack__proxyspec__awx_orig_backend: "{{ docker_stack__awx_orig_backend | d('awx.example.int:8080') }}"

__docker_stack__proxyspec__awx_orig:
  proxy_config: "{{ __docker_stack__awx_orig__proxy_config | d({}, true) }}"
