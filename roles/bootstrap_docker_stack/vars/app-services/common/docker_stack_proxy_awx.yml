---

__docker_stack__proxyspec__awx_backend: "{{ docker_stack__awx_backend
  | d('awx.example.int:8080') }}"

__docker_stack__proxyspec__awx:
  proxy_config: "{{ __docker_stack__awx__proxy_config | d({}, true) }}"
