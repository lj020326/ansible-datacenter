---

__docker_stack__proxyspec__nginx_backend: "{{ docker_stack__nginx_backend | d('nginx.example.int:8080') }}"

__docker_stack__proxyspec__nginx:
  proxy_config: "{{ __docker_stack__nginx__proxy_config | d({}, true) }}"
