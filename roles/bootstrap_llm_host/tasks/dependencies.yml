---
- name: Update apt cache
  when: ansible_facts['pkg_mgr'] == 'apt'
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install required system packages
  ansible.builtin.package:
    name: "{{ bootstrap_llm_host__system_packages }}"
    state: present
