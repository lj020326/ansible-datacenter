---

- name: Brew
  become: no
  homebrew:
    name: "{{ item.brew | default(item.name) }}"
    state: "{{ item.state | default(bootstrap_linux_package_state) }}"
  when: not item.brew_ignore|default('no')|bool
  with_items: "{{ __bootstrap_linux_package_list }}"
