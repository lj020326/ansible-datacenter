---

## Ensure firewalld is installed and started. Set to false if you want to
## run this role for a configuration only task, e.g. to set a temporary rule
firewalld_install: true
firewalld_default_zone: internal

firewalld_enabled: yes

## firewalld.conf changes, e.g. change default zone
firewalld_conf_file: /etc/firewalld/firewalld.conf
firewalld_conf: {}

## Use the firewalld ansible module. Useful for non permanent rules
#firewalld_initial_config: []
firewalld_configs:
  - zone: "{{ firewalld_default_zone }}"
    immediate: 'yes'
    masquerade: 'yes'
    permanent: 'yes'
    state: enabled

## Configure permanent firewalld ipsets (xml config file)
firewalld_ipsets: []
## Configure permanent firewalld services (xml config file)
firewalld_services: []
## Configure permanent firewalld zones (xml config file)
firewalld_zones: []
firewalld_ports: []

firewalld_default_zone_networks:
  ## localhost
  - 127.0.0.0/8
  ## docker
  - 172.0.0.0/8
  ## internal networks
  - 10.0.0.0/8
  - 192.168.0.0/16
