---

- name: restart webmin
  service:
    name: webmin
    state: restarted

- name: "cleanup __webmin_downloads_tempdir"
  ansible.builtin.file:
    state: absent
    path: __webmin_downloads_tempdir.path
