---
# tasks file for inspec - Ubuntu OS

- name: Ubuntu OS | Downloading and installing {{ bootstrap_inspec__app }} {{ bootstrap_inspec__version }}
  apt:
    deb: "{{ bootstrap_inspec__ubuntu_dl_url }}"
