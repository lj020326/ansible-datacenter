---
bootstrap_ansible__default_release: ""

# Valid options include: 'package' or 'pip'.
bootstrap_ansible__install_method: package

# Used only if bootstrap_ansible__install_method is 'pip'. If empty, defaults to latest.
bootstrap_ansible__install_version_pip: ''

# Which extra arguments should be gived to pip
bootstrap_ansible__install_pip_extra_args: ''
