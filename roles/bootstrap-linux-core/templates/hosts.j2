;## ref: https://github.com/ontic/ansible-role-hostname/blob/master/templates/hosts.j2

# {{ ansible_managed }}

127.0.0.1{{ '\t' }}{{ hostname_name_full }}{% if hostname_name_full != hostname_name_short %} {{ hostname_name_short }}{% endif %} localhost localhost.localdomain localhost4 localhost4.localdomain4
{% if hostname_hosts %}
{% for host in hostname_hosts %}
{{ host.ip }}	{{ host.name }} {{ host.aliases | default('') | join(' ') }}
{% endfor %}
{% endif %}

# The following lines are desirable for IPv6 capable hosts
::1{{ '\t' }}{{ hostname_name_full }}{% if hostname_name_full != hostname_name_short %} {{ hostname_name_short }}{% endif %} localhost localhost.localdomain localhost6 localhost4.localdomain6 ip6-localhost ip6-loopback
fe00::0 ip6-localnet
ff00::0 ip6-mcastprefix
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
ff02::3 ip6-allhosts
