---

- name: "(dns) create resolver config file"
  template: 
    src:   "resolv.conf.j2"
    dest: "{{ bootstrap_linux__resolv_conf_dest }}"
    owner: root
    group: root
    mode:  "0644"
    backup: yes
