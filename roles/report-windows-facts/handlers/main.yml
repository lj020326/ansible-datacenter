---

- name: "cleanup report_windows_facts_tempdir"
  delegate_to: "localhost"
  run_once: yes
  when:
    - report_windows_facts_cleanup_tempdir|d(True)|bool
    - report_windows_facts_tempdir.path is defined
  ansible.builtin.file:
    state: absent
    path: report_windows_facts_tempdir.path
