{{ ansible_managed | comment }}

Name: tally2 lockout after failed attempts enforcement
Default: yes
Priority: 1024
Conflicts: cracklib
Auth-Type: Primary
Auth-Initial:
  required      pam_tally2.so deny={{ harden_linux_os_auth_retries }} onerr=fail unlock_time={{ harden_linux_os_auth_lockout_time }}
Account-Type: Primary
Account-Initial:
  required      pam_tally2.so
