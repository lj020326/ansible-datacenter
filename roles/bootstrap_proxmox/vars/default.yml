---
## vars file for bootstrap_proxmox
#pve_base_dir: "/etc/pve"
#pve_cluster_conf: "{{ pve_base_dir }}/corosync.conf"
#
## defaults that need to be host facts
#_pve_cluster_addr0: "{{ ansible_facts['default_ipv4']['address'] }}"
