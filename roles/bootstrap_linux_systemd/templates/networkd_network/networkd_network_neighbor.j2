{% if (ne.address is defined and ne.address |
  ansible.utils.ipaddr('host')) and (ne.link_layer_address is defined and
  (ne.link_layer_address | ansible.utils.ipaddr('host') or
  ne.link_layer_address | ansible.utils.hwaddr())) %}
{{ '[Neighbor]' }}
{{ 'Address=' + ne.address }}
{{ 'LinkLayerAddress=' + ne.link_layer_address }}
{% endif %}
