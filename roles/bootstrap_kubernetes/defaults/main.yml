---
# defaults file for bootstrap_kubernetes
bootstrap_kubernetes__version: "1.28.16-1.1" # Example, check for latest stable 1.28.x or desired version
bootstrap_kubernetes__version_short: "v1.28.16" # Corresponds to the full version, used by kubeadm init
bootstrap_kubernetes__pod_network_cidr: "192.168.0.0/16" # Calico's default, change if using a different CNI
