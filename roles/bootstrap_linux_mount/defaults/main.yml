---

# List of mounts
bootstrap_linux_mount__list: []
# mount state
#bootstrap_linux_mount__state: present
bootstrap_linux_mount__state: mounted

bootstrap_linux_mount__fstab: /etc/fstab
bootstrap_linux_mount__backup_fstab: true

# Toggle to strip swap execution and fstab records (useful for Kubernetes setups)
bootstrap_linux_mount__disable_swap: false

# Automated Swap Management Parameters
bootstrap_linux_mount__swap_disk:
  file: /swap.img
  size: 4G

bootstrap_linux_mount__systemd_service_config: {}

bootstrap_linux_mount__list__tmpdir:
  - name: "/tmp"
    src: "tmpfs"
    fstype: "tmpfs"
    options: "defaults,nosuid,nodev,noexec,mode=1777"
#    options: "rw,size=1G,noatime,nosuid,nodev,noexec,relatime,mode=1777"
