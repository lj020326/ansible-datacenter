---
- name: restart ntp
  service:
    name: "{{ bootstrap_ntp_daemon }}"
    state: restarted
  when: bootstrap_ntp_enabled | bool

- name: restart cron
  service:
    name: "{{ bootstrap_ntp_cron_daemon }}"
    state: restarted
  when: bootstrap_ntp_cron_handler_enabled | bool
