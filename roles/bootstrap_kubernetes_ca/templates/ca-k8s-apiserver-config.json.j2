{
  "signing": {
    "default": {
      "expiry": "{{ bootstrap_kubernetes_ca__ca_apiserver_expiry }}"
    },
    "profiles": {
      "k8s_signing": {
        "usages": [
          "signing",
          "key encipherment",
          "cert sign"
        ],
        "expiry": "{{ bootstrap_kubernetes_ca__ca_apiserver_expiry }}",
        "ca_constraint": {
          "is_ca": true
        }
      },
      "kubernetes": {
        "usages": [
          "signing",
          "key encipherment",
          "server auth",
          "client auth"
        ],
        "expiry": "{{ bootstrap_kubernetes_ca__ca_apiserver_expiry }}"
      }
    }
  }
}
