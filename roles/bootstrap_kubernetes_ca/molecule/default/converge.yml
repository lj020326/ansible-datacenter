---

- name: Setup certificates
  hosts: bootstrap_kubernetes_ca__assets
  become: true
  gather_facts: false
  tasks:
    - name: Generate etcd and K8s TLS certificates
      ansible.builtin.include_role:
        name: githubixx.kubernetes_ca
