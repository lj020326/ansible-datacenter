# {{ ansible_managed }}
# /etc/nsswitch.conf
#
# Example configuration of GNU Name Service Switch functionality.
# If you have the `glibc-doc-reference' and `info' packages installed, try:
# `info libc "Name Service Switch"' for information about this file.

passwd:         compat{% if ldap_client_nss_passwd %}  ldap{% endif %}

group:          compat{% if ldap_client_nss_group %}  ldap{% endif %}

shadow:         compat{% if ldap_client_nss_shadow %}  ldap{% endif %}

hosts:          files dns{% if ldap_client_nss_hosts %}  ldap{% endif %}

networks:       files{% if ldap_client_nss_networks %}  ldap{% endif %}


protocols:      db files
services:       db files
ethers:         db files
rpc:            db files

netgroup:       nis
sudoers:		files{% if ldap_client_sudo %}  ldap{% endif %}
