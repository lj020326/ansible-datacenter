---

#### play 2 - Provisioning ####
- name: "Display host to provision {{ hostvars[inventory_hostname] }}"
  debug:
#    msg: "host = {{ hostvars[inventory_hostname] }}"
    var: hostvars[inventory_hostname]

#- name: Proceed to Provision the MOTD
#  include_role:
#     name: motd
