---

- name: Install etcd cluster
  hosts: kubernetes_etcd
  remote_user: vagrant
  become: true
  gather_facts: true
  tasks:
    - name: Include etcd role
      ansible.builtin.include_role:
        name: bootstrap_etcd
