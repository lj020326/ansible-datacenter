---
- name: Check the kernel FIPS mode
  no_log: true
  ansible.builtin.slurp:
    src: /proc/sys/crypto/fips_enabled
  register: __bootstrap_sshd__kernel_fips_mode
  failed_when: false

- name: Check the userspace FIPS mode
  no_log: true
  ansible.builtin.slurp:
    src: /etc/system-fips
  register: __bootstrap_sshd__userspace_fips_mode
  failed_when: false
