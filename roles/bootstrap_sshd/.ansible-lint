---
exclude_paths:
  - tests/roles/
  - .tox/
  - .markdownlint.yaml
skip_list:
  - var-naming[no-role-prefix]
  - meta-runtime[unsupported-version]
mock_roles:
  - dettonville.sshd.bootstrap_sshd
mock_modules:
  - ansible.posix.mount
