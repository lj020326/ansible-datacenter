---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  listen: "Reload systemd"

- name: Restart act_runner
  ansible.builtin.systemd:
    name: act_runner
    state: restarted
  listen: "Restart act_runner"
