---
bootstrap_ansible_controller__local_users:
  # Example
  # - username: testuser
  #   password: "{{ vaulted_credentials['testuser_password'] }}"
  #   organization: dettonville
  - username: jira_prod
    password: "{{ vaulted_credentials['jira_prod_localuser_password'] }}"
    organization: dettonville
  - username: aem_pipeline
    password: "{{ vaulted_credentials['aem_pipeline_localuser_password'] }}"
    organization: dettonville
