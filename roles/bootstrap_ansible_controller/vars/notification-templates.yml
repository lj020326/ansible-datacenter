---
bootstrap_ansible_controller__notification_templates:
  # - name: Sample Email
  #   description: A sample email notification template
  #   organization: dettonville
  #   notification_type: email
  #   recipients:
  #     - noreply@dettonville.org
  #   environments:
  #     - sandbox

  # - name: Sample webhook
  #   description: A sample webhook notification template
  #   organization: dettonville
  #   notification_type: webhook
  #   notification_configuration:
  #     url: https://webhook.site/6d29dbe9-39c5-47bb-9597-4647d153242e
  #     headers: {}   # headers are required. If no headers, must be set to empty dict.

  - name: INFOSEC Mailing List
    organization: dettonville
    notification_type: email
    recipients:
      - ljohnson@dettonville.org
      - support@dettonville.org
    environments:
      - prod
  
  - name: dettonville - Email INFRA Team
    description: Email ITS Platforms AIM
    organization: dettonville
    notification_type: email
    recipients:
      - platform-automation@dettonville.org
    # notification templates are specified at the job template level so notification templates environment
    # needs to match the job template environment or be across all environments
    # notification_templates_started: "{{ item['notification_templates_started'] | d([], true) }}"
    # notification_templates_success: "{{ item['notification_templates_success'] | d([], true) }}"
    # notification_templates_error: "{{ item['notification_templates_error'] | d([], true) }}"
