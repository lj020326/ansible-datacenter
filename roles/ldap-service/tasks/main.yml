---
## LJ added to enable fw config
- name: Allow ldap traffic through the firewall
  when: firewalld_enabled | bool
  tags: [ firewall-config-cobbler ]
  include_role:
    name: firewall-config
  vars:
    firewalld_services: 
      - name: ldap

