---
- name: Docker | Daemon-reload
  systemd:
    daemon_reload: yes

- name: Docker | Restart Docker
  when: bootstrap_docker__service_manage | bool
  service:
    name: docker
    state: "{{ bootstrap_docker__restart_handler_state }}"
  ignore_errors: "{{ ansible_check_mode }}"
