---
- name: Install Ansible via Pip.
  ansible.builtin.pip:
    name: ansible
    version: "{{ bootstrap_ansible__install_version_pip | d(omit) }}"
    extra_args: "{{ bootstrap_ansible__install_pip_extra_args | d(omit) }}"
