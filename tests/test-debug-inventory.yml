- name: "Run trace var play"
  hosts: all
  gather_facts: false
  connection: local
  tasks:
    - name: Display trace_var
      ansible.builtin.debug:
        var: trace_var

    - name: Display group_names
      ansible.builtin.debug:
        var: group_names
