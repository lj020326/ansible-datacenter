---
## ref: https://github.com/ansible/tower-example
- name: Hello World!
  hosts: all
  tasks:
    - name: Hello World!
      ansible.builtin.command: echo "Hi! Tower is working."
      changed_when: false
