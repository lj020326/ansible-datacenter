all:
  children:
    ansible_localhost:
      hosts:
        localhost: {}
    network_switch:
      hosts:
        powerconnect01: {}
    tower_control_node:
      hosts:
        localhost: {}
        #    vm_template:
        #      hosts:
        #        localhost: {}
    vmware_vcenter:
      hosts:
        vcenter7: {}
