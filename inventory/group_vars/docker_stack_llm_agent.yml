---

docker_stack__service_groups__llm_agent:
  - llm_agent

docker_stack__llm_agent__vikunja_agent_api_token: "{{ vault__vikunja_agent_api_token }}"

docker_stack__llm_agent__vikunja_agent_secret_key: "{{ vault__vikunja_jwt_secret }}"
