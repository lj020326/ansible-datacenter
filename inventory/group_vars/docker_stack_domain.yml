---

docker_stack__internal_root_domain: "{{ service_route_internal_root_domain }}"
docker_stack__external_root_domain: "{{ service_route_external_root_domain }}"

docker_stack__internal_domain: "{{ docker_stack__service_route_name }}.{{ docker_stack__internal_root_domain }}"
docker_stack__external_domain: "{{ docker_stack__service_route_name }}.{{ docker_stack__external_root_domain }}"
#docker_stack__external_domain: "{{ external_domain }}"

docker_stack__ldap_organisation: "{{ ldap_organisation }}"

docker_stack__openldap__domain_sid_root: "{{ ldap_domain_sid_root }}"
