---

bootstrap_docker__swarm_leader_host: "{{ groups['docker_swarm_leader_admin'][0] }}"
docker_stack__swarm_leader_host: "{{ groups['docker_swarm_leader_admin'][0] }}"
bootstrap_docker__swarm_managers: "{{ groups['docker_swarm_leader_admin'] + groups['docker_swarm_manager_admin'] }}"
